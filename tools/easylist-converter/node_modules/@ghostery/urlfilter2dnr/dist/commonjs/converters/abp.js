"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = convert;
const index_js_1 = require("@eyeo/webext-ad-filtering-solution/adblockpluscore/lib/filters/index.js");
const index_js_2 = require("@eyeo/webext-ad-filtering-solution/adblockpluscore/lib/dnr/index.js");
const helpers_js_1 = require("./helpers.js");
const PARAM_MAPPING = {
    ...helpers_js_1.DEFAULT_PARAM_MAPPING,
    'redirect': 'rewrite',
    'redirect-rule': 'rewrite',
    '1p': '~third-party',
};
async function convert(filters) {
    const converter = (0, index_js_2.createConverter)({ isRegexSupported: () => true });
    const rules = [];
    const errors = [];
    let nextId = 1;
    for (const filter of filters) {
        try {
            const normalizedFilter = (0, helpers_js_1.normalizeFilter)((0, index_js_1.normalize)(filter), { mapping: PARAM_MAPPING });
            if (!normalizedFilter) {
                throw new Error('Failed to normalize filter');
            }
            const dnrRules = await converter(normalizedFilter);
            if (dnrRules instanceof index_js_1.FilterParsingError) {
                throw dnrRules;
            }
            if (dnrRules.length > 0) {
                for (const rule of dnrRules) {
                    rule.id = nextId++;
                    rules.push(rule);
                }
            }
            else {
                throw new Error('Unknown problem');
            }
            // eslint-disable-next-line @typescript-eslint/no-explicit-any
        }
        catch (e) {
            errors.push(`Error: "${e.message}" in rule: "${filter}"`);
        }
    }
    return {
        rules: rules.map((rule) => (0, helpers_js_1.normalizeRule)(rule)),
        errors,
    };
}
//# sourceMappingURL=abp.js.map