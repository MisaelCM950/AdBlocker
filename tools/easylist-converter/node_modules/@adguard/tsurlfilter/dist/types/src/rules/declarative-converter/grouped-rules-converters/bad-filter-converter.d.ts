import type { IndexedNetworkRuleWithHash } from '../network-indexed-rule-with-hash.js';
import { type ConvertedRules } from '../converted-result.js';
import { AbstractRuleConverter } from './abstract-rule-converter.js';
/**
 * Just a dummy for $badfilter-rules, because they don't need to be converted.
 */
export declare class BadFilterRulesConverter extends AbstractRuleConverter {
    /**
     * Skips converting bad rules.
     *
     * @param filterId Filter id.
     * @param rules List of indexed rules.
     * @param usedIds Set with already used IDs to exclude duplications in IDs.
     *
     * @returns Empty converted rules.
     */
    convert(filterId: number, rules: IndexedNetworkRuleWithHash[], usedIds: Set<number>): Promise<ConvertedRules>;
}
