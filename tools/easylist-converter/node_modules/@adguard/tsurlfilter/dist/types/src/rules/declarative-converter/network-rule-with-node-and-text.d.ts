import { type NetworkRule as NetworkRuleNode } from '@adguard/agtree';
import { type NetworkRule } from '../network-rule.js';
import { type IRule } from '../rule.js';
/**
 * Network rule with node.
 */
export declare class NetworkRuleWithNodeAndText implements IRule {
    rule: NetworkRule;
    node: NetworkRuleNode;
    text: string;
    /**
     * Creates an instance of NetworkRuleWithNode.
     *
     * @param rule Network rule.
     * @param node Network rule node.
     * @param text Network rule text.
     */
    constructor(rule: NetworkRule, node: NetworkRuleNode, text: string);
    /** @inheritdoc */
    getIndex(): number;
    /** @inheritdoc */
    getFilterListId(): number;
    /** @inheritdoc */
    getText(): string;
}
