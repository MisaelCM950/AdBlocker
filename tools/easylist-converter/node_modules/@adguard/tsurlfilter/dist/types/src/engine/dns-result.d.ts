import { type HostRule } from '../rules/host-rule.js';
import { type NetworkRule } from '../rules/network-rule.js';
/**
 * DnsResult contains a network rule and host rules matching request.
 */
export declare class DnsResult {
    /**
     * BasicRule - a network rule matching the request.
     */
    basicRule: NetworkRule | null;
    /**
     * Host rules.
     */
    hostRules: HostRule[];
}
