import { type IValueListModifier } from './value-list-modifier.js';
export declare enum HTTPMethod {
    GET = "GET",
    POST = "POST",
    PUT = "PUT",
    DELETE = "DELETE",
    PATCH = "PATCH",
    HEAD = "HEAD",
    OPTIONS = "OPTIONS",
    CONNECT = "CONNECT",
    TRACE = "TRACE"
}
/**
 * Method modifier class.
 * Rules with $method modifier will be applied only to requests with specified methods.
 *
 * @see {@link https://adguard.com/kb/general/ad-filtering/create-own-filters/#method-modifier}
 */
export declare class MethodModifier implements IValueListModifier<HTTPMethod> {
    /**
     * Request methods separator.
     */
    private static PIPE_SEPARATOR;
    /**
     * List of permitted methods or null.
     */
    readonly permittedValues: HTTPMethod[] | null;
    /**
     * List of restricted methods or null.
     */
    readonly restrictedValues: HTTPMethod[] | null;
    /**
     * Constructor.
     *
     * @param methodsStr Value of the modifier.
     */
    constructor(methodsStr: string);
    /**
     * Checks if the value is a valid HTTP method.
     *
     * @param value Value to check.
     *
     * @returns True if the value is a valid HTTP method, otherwise false.
     */
    static isHTTPMethod: (value: string) => value is HTTPMethod;
}
