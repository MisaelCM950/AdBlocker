import { BaseValuesModifier } from '../values-modifier.js';
/**
 * The ctag modifier allows to block domains only for specific types of DNS client tags.
 */
export declare class CtagModifier extends BaseValuesModifier {
    /**
     * The list of allowed tags.
     */
    private static ALLOWED_TAGS;
    /**
     * Constructor.
     *
     * @param value Value of the modifier.
     */
    constructor(value: string);
    /**
     * Validates tag values.
     */
    private validate;
}
