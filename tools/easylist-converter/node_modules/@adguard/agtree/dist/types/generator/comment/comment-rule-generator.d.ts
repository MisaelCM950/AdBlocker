import { type AnyCommentRule } from '../../nodes/index.js';
import { BaseGenerator } from '../base-generator.js';
/**
 * `CommentRuleGenerator` is responsible for generating any comment-like adblock rules.
 */
export declare class CommentRuleGenerator extends BaseGenerator {
    /**
     * Converts a comment rule node to a string.
     *
     * @param node Comment rule node.
     *
     * @returns Raw string.
     */
    static generate(node: AnyCommentRule): string;
}
